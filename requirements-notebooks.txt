# The lab notebooks only. Kept out of requirements.lock because the application
# and its tests do not need Jupyter — the classroom image stays small, and a
# participant who only runs `make` never installs any of this.
#
#   pip install -r requirements.lock -r requirements-notebooks.txt
#
jupyterlab==4.6.3
ipykernel==7.3.0
nbclient==0.11.0
nbformat==5.11.1
